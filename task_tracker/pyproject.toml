[project]
name = "task-tracker"
version = "0.1.0"
description = "Simple CLI task tracker — demo project for AI dev team workflow"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]
